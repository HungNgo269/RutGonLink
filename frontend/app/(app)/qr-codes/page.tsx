import { SectionPlaceholder } from "@/components/app-shell/section-placeholder";

export default function QrCodesPage() {
  return (
    <SectionPlaceholder
      eyebrow="QR Codes"
      title="Generate scannable destinations for every campaign."
      description="Use this area for QR code creation, print exports, and campaign-specific destinations while the persistent layout keeps navigation stable."
    />
  );
}
