import { SectionPlaceholder } from "@/components/app-shell/section-placeholder";

export default function PagesPage() {
  return (
    <SectionPlaceholder
      eyebrow="Pages"
      title="Launch compact landing pages around your links."
      description="This placeholder route gives you a dedicated section for microsites, CTAs, and destination pages without rebuilding the shell."
    />
  );
}
