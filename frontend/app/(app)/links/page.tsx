import { SectionPlaceholder } from "@/components/app-shell/section-placeholder";

export default function LinksPage() {
  return (
    <SectionPlaceholder
      eyebrow="Links"
      title="Manage and review every shortened link."
      description="Track inventory, scan recent activity, and keep your link workspace organized without leaving the shared dashboard shell."
    />
  );
}
